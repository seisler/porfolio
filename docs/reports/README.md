# Reports

Auto-generated bug investigation reports produced by the `sneak` agent
(`~/.claude/agents/sneak.md`), one per Trello ticket investigated:
`<card-id>-<slug>.md`.

These are investigation findings (root cause, affected files, suggested
approach) — not the fix itself.
